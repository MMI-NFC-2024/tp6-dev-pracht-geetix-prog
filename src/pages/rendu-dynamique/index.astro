---
import Layout from "../../layouts/Layout.astro";
import penguins from "../../assets/penguins.json";
import cars from "../../assets/cars.json";

const species = [...new Set(penguins.map((p) => p.species))];

const speciesCount = species.map((s) => ({
    name: s,
    count: penguins.filter((p) => p.species === s).length,
}));

const years = [...new Set(cars.map((c) => c.year).filter(Boolean))].sort((a, b) => a - b);

const yearsCount = years.map((y) => ({
    year: y,
    count: cars.filter((c) => c.year === y).length,
}));
---

<Layout>
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        <nav class="flex mb-8" aria-label="Breadcrumb">
            <ol class="inline-flex items-center space-x-1 md:space-x-3">
                <li class="inline-flex items-center">
                    <a href="/" class="inline-flex items-center text-sm font-medium text-gray-700 hover:text-blue-600">
                        <svg class="w-4 h-4 mr-2" fill="currentColor" viewBox="0 0 20 20">
                            <path d="m19.707 9.293-2-2-7-7a1 1 0 0 0-1.414 0l-7 7-2 2a1 1 0 0 0 1.414 1.414L2 10.414V18a2 2 0 0 0 2 2h3a1 1 0 0 0 1-1v-4a1 1 0 0 1 1-1h2a1 1 0 0 1 1 1v4a1 1 0 0 0 1 1h3a2 2 0 0 0 2-2v-7.586l.293.293a1 1 0 0 0 1.414-1.414Z"/>
                        </svg>
                        Accueil
                    </a>
                </li>
                <li aria-current="page">
                    <div class="flex items-center">
                        <svg class="w-6 h-6 text-gray-400" fill="currentColor" viewBox="0 0 20 20">
                            <path fill-rule="evenodd" d="M7.293 14.707a1 1 0 010-1.414L10.586 10 7.293 6.707a1 1 0 011.414-1.414l4 4a1 1 0 010 1.414l-4 4a1 1 0 01-1.414 0z" clip-rule="evenodd"></path>
                        </svg>
                        <span class="ml-1 text-sm font-medium text-gray-500 md:ml-2">Rendu Dynamique</span>
                    </div>
                </li>
            </ol>
        </nav>

        <div class="bg-gradient-to-r from-purple-50 to-pink-50 rounded-lg p-6 mb-8">
            <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between">
                <div>
                    <h1 class="text-3xl font-bold text-gray-900 mb-2">
                        Espèces de Pingouins & Années de Voitures
                    </h1>
                    <p class="text-lg text-gray-600">
                        Explorez les données par espèce et par année - Pages générées dynamiquement
                    </p>
                </div>
                <div class="mt-4 sm:mt-0">
                    <span class="inline-flex items-center px-3 py-1 rounded-full text-sm font-medium bg-purple-100 text-purple-800">
                        🚀 Rendu Dynamique
                    </span>
                </div>
            </div>
        </div>

        <div class="mb-8">
            <h2 class="text-2xl font-bold text-gray-900 mb-6 flex items-center">
                🐧 Espèces de Pingouins
                <span class="ml-3 text-sm font-normal text-gray-500">({species.length} espèces)</span>
            </h2>
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
                {speciesCount.map((species) => (
                    <div class="bg-white rounded-lg shadow-sm border border-gray-200 overflow-hidden hover:shadow-lg transition-shadow duration-200">
                        <div class="p-6">
                            <div class="flex items-center justify-between mb-4">
                                <h3 class="text-xl font-semibold text-gray-900">{species.name}</h3>
                                <div class="w-3 h-3 bg-purple-400 rounded-full"></div>
                            </div>
                            <p class="text-gray-600 mb-4">
                                {species.count} pingouins dans nos données
                            </p>
                            <a 
                                href={`/rendu-dynamique/especes/${species.name}`}
                                class="inline-flex items-center justify-center w-full px-4 py-2 border border-transparent text-sm font-medium rounded-md text-white bg-purple-600 hover:bg-purple-700 transition-colors duration-200"
                            >
                                Voir les données
                                <svg class="ml-2 w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                                </svg>
                            </a>
                        </div>
                    </div>
                ))}
            </div>
        </div>

        <div class="mb-8">
            <h2 class="text-2xl font-bold text-gray-900 mb-6 flex items-center">
                🚗 Années de Voitures
                <span class="ml-3 text-sm font-normal text-gray-500">({years.length} années)</span>
            </h2>
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4">
                {yearsCount.map((year) => (
                    <div class="bg-white rounded-lg shadow-sm border border-gray-200 overflow-hidden hover:shadow-lg transition-shadow duration-200">
                        <div class="p-4">
                            <div class="flex items-center justify-between mb-3">
                                <h3 class="text-lg font-semibold text-gray-900">{year.year}</h3>
                                <div class="w-3 h-3 bg-pink-400 rounded-full"></div>
                            </div>
                            <p class="text-gray-600 mb-3 text-sm">
                                {year.count} voitures
                            </p>
                            <a 
                                href={`/rendu-dynamique/annees/${year.year}`}
                                class="inline-flex items-center justify-center w-full px-3 py-2 border border-transparent text-sm font-medium rounded-md text-white bg-pink-600 hover:bg-pink-700 transition-colors duration-200"
                            >
                                Voir les données
                                <svg class="ml-1 w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                                </svg>
                            </a>
                        </div>
                    </div>
                ))}
            </div>
        </div>

        <div class="bg-gray-50 rounded-lg p-6">
            <div class="flex items-center mb-4">
                <svg class="w-6 h-6 text-purple-600 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                </svg>
                <h3 class="text-lg font-semibold text-gray-900">À propos du rendu dynamique</h3>
            </div>
            <p class="text-gray-600">
                Ces pages sont générées à la demande grâce à <code class="bg-gray-200 px-2 py-1 rounded text-sm">prerender = false</code>. 
                Idéal pour du contenu qui change fréquemment ou des pages personnalisées.
            </p>
        </div>
    </div>
</Layout>
